<Window x:Class="Robot.App.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helix="http://helix-toolkit.org/wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="RobotControl MVP" Height="820" Width="1200">
    <Window.Resources>
        <LinearGradientBrush x:Key="SimulationViewportBackgroundBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FF0A2A52" Offset="0"/>
            <GradientStop Color="#FF051325" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0" Margin="0,0,0,8">
            <Button x:Name="ConnectButton" Content="Connect" Width="100" Margin="0,0,8,8" Click="ConnectButton_OnClick"/>
            <Button x:Name="MoveLinearButton" Content="MoveLinear" Width="110" Margin="0,0,8,8" Click="MoveLinearButton_OnClick"/>
            <Button x:Name="MoveArcButton" Content="MoveArc3D_3Point" Width="140" Margin="0,0,8,8" Click="MoveArcButton_OnClick"/>
            <Button x:Name="ResetAlarmButton" Content="ResetAlarm" Width="110" Margin="0,0,8,8" Click="ResetAlarmButton_OnClick"/>
            <CheckBox x:Name="EStopCheckBox" Content="Sim E-Stop" Margin="0,4,12,0" Checked="SafetyIo_OnChanged" Unchecked="SafetyIo_OnChanged"/>
            <CheckBox x:Name="DoorOpenCheckBox" Content="Sim Door Open" Margin="0,4,12,0" Checked="SafetyIo_OnChanged" Unchecked="SafetyIo_OnChanged"/>
            <TextBlock x:Name="AlarmTextBlock" VerticalAlignment="Center" FontWeight="Bold" Foreground="Green" Text="Alarm: None" />
        </WrapPanel>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,8,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <WrapPanel Grid.Row="0" Margin="0,0,0,4">
                    <TextBlock VerticalAlignment="Center" Text="Target X"/>
                    <TextBox x:Name="TargetXTextBox" Width="70" Margin="4,0,8,0" Text="100"/>
                    <TextBlock VerticalAlignment="Center" Text="Y"/>
                    <TextBox x:Name="TargetYTextBox" Width="70" Margin="4,0,8,0" Text="100"/>
                    <TextBlock VerticalAlignment="Center" Text="Z"/>
                    <TextBox x:Name="TargetZTextBox" Width="70" Margin="4,0,8,0" Text="100"/>
                    <TextBlock VerticalAlignment="Center" Text="Rx"/>
                    <TextBox x:Name="TargetRxTextBox" Width="70" Margin="4,0,8,0" Text="30"/>
                    <TextBlock VerticalAlignment="Center" Text="Ry"/>
                    <TextBox x:Name="TargetRyTextBox" Width="70" Margin="4,0,8,0" Text="20"/>
                    <TextBlock VerticalAlignment="Center" Text="Rz"/>
                    <TextBox x:Name="TargetRzTextBox" Width="70" Margin="4,0,8,0" Text="10"/>
                    <TextBlock VerticalAlignment="Center" Text="Feed(mm/s)"/>
                    <TextBox x:Name="FeedTextBox" Width="70" Margin="4,0,8,0" Text="120"/>
                </WrapPanel>

                <WrapPanel Grid.Row="1">
                    <TextBlock VerticalAlignment="Center" Text="Via X"/>
                    <TextBox x:Name="ViaXTextBox" Width="70" Margin="4,0,8,0" Text="60"/>
                    <TextBlock VerticalAlignment="Center" Text="Y"/>
                    <TextBox x:Name="ViaYTextBox" Width="70" Margin="4,0,8,0" Text="140"/>
                    <TextBlock VerticalAlignment="Center" Text="Z"/>
                    <TextBox x:Name="ViaZTextBox" Width="70" Margin="4,0,8,0" Text="80"/>
                    <TextBlock x:Name="PoseTextBlock" VerticalAlignment="Center" Margin="12,0,0,0" Text="Pose: X=0, Y=0, Z=0, Rx=0, Ry=0, Rz=0"/>
                </WrapPanel>
            </Grid>

            <GroupBox Grid.Column="1" Header="Loaded Drivers Summary">
                <ListBox x:Name="DriverSummaryListBox" MinHeight="80" />
            </GroupBox>
        </Grid>

        <TabControl Grid.Row="2">
            <TabItem Header="Top (XY)">
                <Canvas x:Name="TopCanvas" Background="#FF1E1E1E" />
            </TabItem>
            <TabItem Header="Front (XZ)">
                <Canvas x:Name="FrontCanvas" Background="#FF1E1E1E" />
            </TabItem>
            <TabItem Header="Side (YZ)">
                <Canvas x:Name="SideCanvas" Background="#FF1E1E1E" />
            </TabItem>
            <TabItem Header="3D">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <WrapPanel Grid.Row="0" Margin="0,0,0,8">
                        <Button Content="Front Camera" Width="110" Margin="0,0,8,6" Click="FrontCameraButton_OnClick"/>
                        <Button Content="Side Camera" Width="110" Margin="0,0,8,6" Click="SideCameraButton_OnClick"/>
                        <Button Content="Work Camera" Width="110" Margin="0,0,8,6" Click="WorkCameraButton_OnClick"/>
                        <Button Content="Capture Front PNG" Width="130" Margin="0,0,8,6" Click="CaptureFrontButton_OnClick"/>
                        <Button Content="Capture Side PNG" Width="130" Margin="0,0,8,6" Click="CaptureSideButton_OnClick"/>
                        <Button Content="Capture Work PNG" Width="130" Margin="0,0,8,6" Click="CaptureWorkButton_OnClick"/>
                    </WrapPanel>

                    <Grid Grid.Row="1" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,8,0">
                            <CheckBox x:Name="FixedSeedCheckBox" Content="Fixed Seed" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock VerticalAlignment="Center" Text="Seed"/>
                            <TextBox x:Name="SeedTextBox" Width="80" Margin="4,0,8,0" Text="1234"/>
                            <Button Content="Randomize Workpiece" Width="160" Click="RandomizeWorkpieceButton_OnClick"/>
                            <TextBlock x:Name="WorkpieceTextBlock" VerticalAlignment="Center" Margin="12,0,0,0" Text="Workpiece: (not set)"/>
                        </StackPanel>

                        <TextBlock x:Name="CollisionTextBlock"
                                   Grid.Column="1"
                                   VerticalAlignment="Center"
                                   FontWeight="Bold"
                                   Foreground="Green"
                                   Text="Collision: None"/>
                    </Grid>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="240"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="Joint Sliders (deg)" FontWeight="Bold" Margin="0,0,0,8"/>

                            <TextBlock Text="q1"/>
                            <Slider x:Name="Joint1Slider" Minimum="-180" Maximum="180" Value="0" ValueChanged="JointSlider_OnValueChanged"/>
                            <TextBlock x:Name="Joint1ValueTextBlock" Margin="0,0,0,6" Text="0.0°"/>

                            <TextBlock Text="q2"/>
                            <Slider x:Name="Joint2Slider" Minimum="-180" Maximum="180" Value="-90" ValueChanged="JointSlider_OnValueChanged"/>
                            <TextBlock x:Name="Joint2ValueTextBlock" Margin="0,0,0,6" Text="-90.0°"/>

                            <TextBlock Text="q3"/>
                            <Slider x:Name="Joint3Slider" Minimum="-180" Maximum="180" Value="90" ValueChanged="JointSlider_OnValueChanged"/>
                            <TextBlock x:Name="Joint3ValueTextBlock" Margin="0,0,0,6" Text="90.0°"/>

                            <TextBlock Text="q4"/>
                            <Slider x:Name="Joint4Slider" Minimum="-180" Maximum="180" Value="0" ValueChanged="JointSlider_OnValueChanged"/>
                            <TextBlock x:Name="Joint4ValueTextBlock" Margin="0,0,0,6" Text="0.0°"/>

                            <TextBlock Text="q5"/>
                            <Slider x:Name="Joint5Slider" Minimum="-180" Maximum="180" Value="0" ValueChanged="JointSlider_OnValueChanged"/>
                            <TextBlock x:Name="Joint5ValueTextBlock" Margin="0,0,0,6" Text="0.0°"/>

                            <TextBlock Text="q6"/>
                            <Slider x:Name="Joint6Slider" Minimum="-180" Maximum="180" Value="0" ValueChanged="JointSlider_OnValueChanged"/>
                            <TextBlock x:Name="Joint6ValueTextBlock" Text="0.0°"/>
                        </StackPanel>

                        <Border Grid.Column="1"
                                BorderBrush="#FF2C2C2C"
                                BorderThickness="1"
                                Background="{StaticResource SimulationViewportBackgroundBrush}">
                            <helix:HelixViewport3D x:Name="SimViewport"
                                                   Background="{StaticResource SimulationViewportBackgroundBrush}"
                                                   ShowCoordinateSystem="False"
                                                   ZoomExtentsWhenLoaded="True"/>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
